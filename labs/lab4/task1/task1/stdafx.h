#pragma once

#include "targetver.h"

#include <iostream>
#include <string>
#include <sstream>
#include <fstream>
#include <iomanip>

#define _USE_MATH_DEFINES
#include <math.h>

const int DEFAULT_DENSITY = 1;
const int DEFAULT_RADIUS = 1;
const int DEFAULT_WIDTH = 1;
const int DEFAULT_HEIGTH = 1;
const int DEFAULT_DEPTH = 1;
const int DEFAULT_PRECISION = 2;